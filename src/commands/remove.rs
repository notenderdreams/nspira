pub fn run(id: &u32) {
    // 1. Fetch project
    // let project = crate::db::get_project_by_id(id);

    // if project.is_none() {
    //     eprintln!("No project found with ID {}", id);
    //     return;
    // }

    //  2. Delete from DB
    // crate::db::remove_project(id);

    println!("Removed project with ID {}", id);
}
